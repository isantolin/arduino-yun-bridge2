# --- BEGIN GENERATED PACKETS --- DO NOT EDIT (auto-generated from spec.toml)
{% for packet in packets %}

class {{ packet.class_name }}(BaseStruct, frozen=True):
{%- for field in packet.fields %}
    {{ field.name }}: {{ field.python_type }}
{%- endfor %}

{% endfor %}

COMMAND_TO_PACKET: dict[int, type[BaseStruct]] = {
{%- for cmd_name, packet_cls in command_to_packet.items() %}
    Command.{{ cmd_name }}.value: {{ packet_cls }},
{%- endfor %}
}
# --- END GENERATED PACKETS ---
