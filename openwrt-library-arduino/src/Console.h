#pragma once

// Backward compatibility header for legacy sketches that include <Console.h>.
// The functionality now lives in Bridge.h alongside the ConsoleClass definition.
// Including this header ensures existing code keeps compiling without changes.
#include "Bridge.h"
